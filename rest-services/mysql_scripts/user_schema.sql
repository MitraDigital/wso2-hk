USE `wso2apimgt`;

-- Dumping structure for table  wso2apimgt.IDN_USER_PASS_CHANGES
CREATE TABLE `IDN_USER_PASS_CHANGES` (
  `REQUEST_ID`   CHAR(50)  NOT NULL,
  `USER_ID`      CHAR(100) NOT NULL,
  `IS_CHANGED`   INT(1)    NOT NULL DEFAULT '0',
  `REQUEST_TIME` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`REQUEST_ID`),
  UNIQUE INDEX `REQUEST_ID` (`REQUEST_ID`)
)
  ENGINE = InnoDB
  DEFAULT CHARSET = utf8;
